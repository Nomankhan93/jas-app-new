# Patch P — Auto clear notification badge on Updates page

## Included changes

1. Opening `/notifications` automatically marks unread notifications as read.
2. Header avatar badge clears after the Updates page loads.
3. Updates dropdown badge clears after the Updates page loads.
4. Load More still works with 50-record pagination.
5. Existing notification badges and action button polish remain included.
6. No database migration.
